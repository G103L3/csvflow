# csvflow

Lightweight ETL library and CLI for CSV processing: filter, join, transform, export to JSON/SQL

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python main.py --help
```
